<?xml version="1.0" ?>

<container xmlns="http://symfony.com/schema/dic/services"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://symfony.com/schema/dic/services http://symfony.com/schema/dic/services/services-1.0.xsd">

    <!-- We need a service that can takes:
                - the current request
                - the needed role
            to check :
                - if the token exists in redis
                - returns the associated user id and role(s)
                - gives the response (authorized or not / true or false)

    <parameters>
        <parameter key="cubbyhole_api.auth.class">Cubbyhole\ApiBundle\Auth\</parameter>
    </parameters>

    <services>
        <service id="cubbyhole_api.auth" class="%cubbyhole_api.auth.class%">
            <argument type="service" id="service_id" />
            <argument>plain_value</argument>
            <argument>%parameter_name%</argument>
        </service>
    </services>-->
</container>
